// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/shiqinfeng1/goframe-ddd/internal/demo/infrastructure/repositories/ent/filetransfertask"
	"github.com/shiqinfeng1/goframe-ddd/internal/demo/infrastructure/repositories/ent/predicate"
)

// FileTransferTaskQuery is the builder for querying FileTransferTask entities.
type FileTransferTaskQuery struct {
	config
	ctx        *QueryContext
	order      []filetransfertask.OrderOption
	inters     []Interceptor
	predicates []predicate.FileTransferTask
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the FileTransferTaskQuery builder.
func (fttq *FileTransferTaskQuery) Where(ps ...predicate.FileTransferTask) *FileTransferTaskQuery {
	fttq.predicates = append(fttq.predicates, ps...)
	return fttq
}

// Limit the number of records to be returned by this query.
func (fttq *FileTransferTaskQuery) Limit(limit int) *FileTransferTaskQuery {
	fttq.ctx.Limit = &limit
	return fttq
}

// Offset to start from.
func (fttq *FileTransferTaskQuery) Offset(offset int) *FileTransferTaskQuery {
	fttq.ctx.Offset = &offset
	return fttq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (fttq *FileTransferTaskQuery) Unique(unique bool) *FileTransferTaskQuery {
	fttq.ctx.Unique = &unique
	return fttq
}

// Order specifies how the records should be ordered.
func (fttq *FileTransferTaskQuery) Order(o ...filetransfertask.OrderOption) *FileTransferTaskQuery {
	fttq.order = append(fttq.order, o...)
	return fttq
}

// First returns the first FileTransferTask entity from the query.
// Returns a *NotFoundError when no FileTransferTask was found.
func (fttq *FileTransferTaskQuery) First(ctx context.Context) (*FileTransferTask, error) {
	nodes, err := fttq.Limit(1).All(setContextOp(ctx, fttq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{filetransfertask.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (fttq *FileTransferTaskQuery) FirstX(ctx context.Context) *FileTransferTask {
	node, err := fttq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first FileTransferTask ID from the query.
// Returns a *NotFoundError when no FileTransferTask ID was found.
func (fttq *FileTransferTaskQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = fttq.Limit(1).IDs(setContextOp(ctx, fttq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{filetransfertask.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (fttq *FileTransferTaskQuery) FirstIDX(ctx context.Context) int {
	id, err := fttq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single FileTransferTask entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one FileTransferTask entity is found.
// Returns a *NotFoundError when no FileTransferTask entities are found.
func (fttq *FileTransferTaskQuery) Only(ctx context.Context) (*FileTransferTask, error) {
	nodes, err := fttq.Limit(2).All(setContextOp(ctx, fttq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{filetransfertask.Label}
	default:
		return nil, &NotSingularError{filetransfertask.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (fttq *FileTransferTaskQuery) OnlyX(ctx context.Context) *FileTransferTask {
	node, err := fttq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only FileTransferTask ID in the query.
// Returns a *NotSingularError when more than one FileTransferTask ID is found.
// Returns a *NotFoundError when no entities are found.
func (fttq *FileTransferTaskQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = fttq.Limit(2).IDs(setContextOp(ctx, fttq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{filetransfertask.Label}
	default:
		err = &NotSingularError{filetransfertask.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (fttq *FileTransferTaskQuery) OnlyIDX(ctx context.Context) int {
	id, err := fttq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of FileTransferTasks.
func (fttq *FileTransferTaskQuery) All(ctx context.Context) ([]*FileTransferTask, error) {
	ctx = setContextOp(ctx, fttq.ctx, ent.OpQueryAll)
	if err := fttq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*FileTransferTask, *FileTransferTaskQuery]()
	return withInterceptors[[]*FileTransferTask](ctx, fttq, qr, fttq.inters)
}

// AllX is like All, but panics if an error occurs.
func (fttq *FileTransferTaskQuery) AllX(ctx context.Context) []*FileTransferTask {
	nodes, err := fttq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of FileTransferTask IDs.
func (fttq *FileTransferTaskQuery) IDs(ctx context.Context) (ids []int, err error) {
	if fttq.ctx.Unique == nil && fttq.path != nil {
		fttq.Unique(true)
	}
	ctx = setContextOp(ctx, fttq.ctx, ent.OpQueryIDs)
	if err = fttq.Select(filetransfertask.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (fttq *FileTransferTaskQuery) IDsX(ctx context.Context) []int {
	ids, err := fttq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (fttq *FileTransferTaskQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, fttq.ctx, ent.OpQueryCount)
	if err := fttq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, fttq, querierCount[*FileTransferTaskQuery](), fttq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (fttq *FileTransferTaskQuery) CountX(ctx context.Context) int {
	count, err := fttq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (fttq *FileTransferTaskQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, fttq.ctx, ent.OpQueryExist)
	switch _, err := fttq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (fttq *FileTransferTaskQuery) ExistX(ctx context.Context) bool {
	exist, err := fttq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the FileTransferTaskQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (fttq *FileTransferTaskQuery) Clone() *FileTransferTaskQuery {
	if fttq == nil {
		return nil
	}
	return &FileTransferTaskQuery{
		config:     fttq.config,
		ctx:        fttq.ctx.Clone(),
		order:      append([]filetransfertask.OrderOption{}, fttq.order...),
		inters:     append([]Interceptor{}, fttq.inters...),
		predicates: append([]predicate.FileTransferTask{}, fttq.predicates...),
		// clone intermediate query.
		sql:  fttq.sql.Clone(),
		path: fttq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		TaskID string `json:"task_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.FileTransferTask.Query().
//		GroupBy(filetransfertask.FieldTaskID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (fttq *FileTransferTaskQuery) GroupBy(field string, fields ...string) *FileTransferTaskGroupBy {
	fttq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &FileTransferTaskGroupBy{build: fttq}
	grbuild.flds = &fttq.ctx.Fields
	grbuild.label = filetransfertask.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		TaskID string `json:"task_id,omitempty"`
//	}
//
//	client.FileTransferTask.Query().
//		Select(filetransfertask.FieldTaskID).
//		Scan(ctx, &v)
func (fttq *FileTransferTaskQuery) Select(fields ...string) *FileTransferTaskSelect {
	fttq.ctx.Fields = append(fttq.ctx.Fields, fields...)
	sbuild := &FileTransferTaskSelect{FileTransferTaskQuery: fttq}
	sbuild.label = filetransfertask.Label
	sbuild.flds, sbuild.scan = &fttq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a FileTransferTaskSelect configured with the given aggregations.
func (fttq *FileTransferTaskQuery) Aggregate(fns ...AggregateFunc) *FileTransferTaskSelect {
	return fttq.Select().Aggregate(fns...)
}

func (fttq *FileTransferTaskQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range fttq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, fttq); err != nil {
				return err
			}
		}
	}
	for _, f := range fttq.ctx.Fields {
		if !filetransfertask.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if fttq.path != nil {
		prev, err := fttq.path(ctx)
		if err != nil {
			return err
		}
		fttq.sql = prev
	}
	return nil
}

func (fttq *FileTransferTaskQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*FileTransferTask, error) {
	var (
		nodes = []*FileTransferTask{}
		_spec = fttq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*FileTransferTask).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &FileTransferTask{config: fttq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, fttq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (fttq *FileTransferTaskQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := fttq.querySpec()
	_spec.Node.Columns = fttq.ctx.Fields
	if len(fttq.ctx.Fields) > 0 {
		_spec.Unique = fttq.ctx.Unique != nil && *fttq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, fttq.driver, _spec)
}

func (fttq *FileTransferTaskQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(filetransfertask.Table, filetransfertask.Columns, sqlgraph.NewFieldSpec(filetransfertask.FieldID, field.TypeInt))
	_spec.From = fttq.sql
	if unique := fttq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if fttq.path != nil {
		_spec.Unique = true
	}
	if fields := fttq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, filetransfertask.FieldID)
		for i := range fields {
			if fields[i] != filetransfertask.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := fttq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := fttq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := fttq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := fttq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (fttq *FileTransferTaskQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(fttq.driver.Dialect())
	t1 := builder.Table(filetransfertask.Table)
	columns := fttq.ctx.Fields
	if len(columns) == 0 {
		columns = filetransfertask.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if fttq.sql != nil {
		selector = fttq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if fttq.ctx.Unique != nil && *fttq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range fttq.predicates {
		p(selector)
	}
	for _, p := range fttq.order {
		p(selector)
	}
	if offset := fttq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := fttq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// FileTransferTaskGroupBy is the group-by builder for FileTransferTask entities.
type FileTransferTaskGroupBy struct {
	selector
	build *FileTransferTaskQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (fttgb *FileTransferTaskGroupBy) Aggregate(fns ...AggregateFunc) *FileTransferTaskGroupBy {
	fttgb.fns = append(fttgb.fns, fns...)
	return fttgb
}

// Scan applies the selector query and scans the result into the given value.
func (fttgb *FileTransferTaskGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, fttgb.build.ctx, ent.OpQueryGroupBy)
	if err := fttgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*FileTransferTaskQuery, *FileTransferTaskGroupBy](ctx, fttgb.build, fttgb, fttgb.build.inters, v)
}

func (fttgb *FileTransferTaskGroupBy) sqlScan(ctx context.Context, root *FileTransferTaskQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(fttgb.fns))
	for _, fn := range fttgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*fttgb.flds)+len(fttgb.fns))
		for _, f := range *fttgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*fttgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := fttgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// FileTransferTaskSelect is the builder for selecting fields of FileTransferTask entities.
type FileTransferTaskSelect struct {
	*FileTransferTaskQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ftts *FileTransferTaskSelect) Aggregate(fns ...AggregateFunc) *FileTransferTaskSelect {
	ftts.fns = append(ftts.fns, fns...)
	return ftts
}

// Scan applies the selector query and scans the result into the given value.
func (ftts *FileTransferTaskSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ftts.ctx, ent.OpQuerySelect)
	if err := ftts.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*FileTransferTaskQuery, *FileTransferTaskSelect](ctx, ftts.FileTransferTaskQuery, ftts, ftts.inters, v)
}

func (ftts *FileTransferTaskSelect) sqlScan(ctx context.Context, root *FileTransferTaskQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ftts.fns))
	for _, fn := range ftts.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ftts.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ftts.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
